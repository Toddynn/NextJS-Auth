import { NextApiRequest, NextApiResponse } from 'next';
import { Options } from 'swagger-jsdoc';
declare type SwaggerOptions = Options & {
    apiFolder?: string;
};
/**
 * Create swagger JSON
 * @param options.openApiVersion Open API version {3.0.0}
 * @param options.apiFolder NextJS API folder {pages/api}
 * @param options.title Title
 * @param options.version Version
 * @returns Swagger JSON Spec
 */
export declare function createSwaggerSpec({ apiFolder, ...swaggerOptions }?: SwaggerOptions): object;
/**
 * withSwagger middleware
 * @param options.openApiVersion Open API version {3.0.0}
 * @param options.apiFolder NextJS API folder {pages/api}
 * @param options.title Title
 * @param options.version Version
 * @returns
 */
export declare function withSwagger({ apiFolder, ...swaggerOptions }?: SwaggerOptions): () => (_req: NextApiRequest, res: NextApiResponse) => void;
export {};
